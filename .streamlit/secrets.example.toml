# Copy this file to .streamlit/secrets.toml and replace every placeholder.
# Use a Supabase anon/publishable key only; never use a service-role key in this app.
SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_KEY = "your-supabase-anon-or-publishable-key"
GROQ_API_KEY = "your-groq-api-key"
# Generate a random value with: python -c "import secrets; print(secrets.token_urlsafe(32))"
APP_SESSION_INTERNAL_KEY = "replace-with-a-random-session-service-secret"
# For local development, leave these values unchanged. A production deployment
# must use HTTPS and set SESSION_COOKIE_SECURE = true.
APP_BASE_URL = "http://localhost:8502"
SESSION_SERVICE_URL = "http://127.0.0.1:8503"
SESSION_SERVICE_PUBLIC_URL = "http://localhost:8503"
SESSION_COOKIE_SECURE = false
